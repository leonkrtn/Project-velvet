// PDF-Export für Solo-Brautpaare — nutzt dieselbe Server-Page wie der
// Veranstalter (Rollen-Check dort erlaubt brautpaar_solo), gerendert
// innerhalb der Brautpaar-Shell.
export { default } from '@/app/veranstalter/[eventId]/pdf-export/page'
